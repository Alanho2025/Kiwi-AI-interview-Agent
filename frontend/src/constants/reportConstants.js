/**
 * Report-related constants
 */

/**
 * Timeout for export audit API calls (ms)
 * Prevents browser downloads from appearing frozen if audit call is slow
 */
export const EXPORT_AUDIT_TIMEOUT_MS = 6000;

// Made with Bob
