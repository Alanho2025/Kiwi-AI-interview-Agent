# Session service export bug fixed\n\n- [x] 1. Added `export const updateSession = updateSessionById;` and `export const softDeleteOwnedSession = softDeleteSessionById;` to backend/src/services/session/sessionLifecycleService.js\n- [x] 2. Verified: Backend server now starts successfully with `cd backend && npm run dev`\n- [x] 3. Task complete\n\nServer is running without syntax errors."

